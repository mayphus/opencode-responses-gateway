[project]
name = "opencode-go-proxy"
version = "0.1.2"
description = "Use your OpenCode Go subscription in Codex — local Responses-to-Chat-Completions bridge"
readme = "README.md"
authors = [{ name = "Kartik Kabadi", email = "1kartikkabadi1@gmail.com" }]
license = "MIT"
requires-python = ">=3.11"
dependencies = []
keywords = ["codex", "opencode", "responses-api", "chat-completions", "agent", "proxy"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.urls]
Homepage = "https://github.com/zhengsanniu/opencode-go-proxy"
Repository = "https://github.com/zhengsanniu/opencode-go-proxy"
Issues = "https://github.com/zhengsanniu/opencode-go-proxy/issues"

[project.scripts]
opencode-go-proxy = "opencode_go_proxy.app:main"
opencode-go-verify = "opencode_go_proxy.verify:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = ["pytest>=8"]
